# Encye Lexicon — Backend

FastAPI + SQLModel backend with a Google ADK summarizer agent.

```bash
uv sync
uv run uvicorn main:app --reload   # http://localhost:8000
```

See the [project README](../README.md) for full setup, environment variables,
and how to run the frontend alongside it.
